[project]
name = "iris-service"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Qing Wong", email = "qingwong@berkeley.edu" }
]
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.141.1",
    "joblib>=1.6.0",
    "scikit-learn>=1.9.0",
    "uvicorn[standard]>=0.52.4",
]

[build-system]
requires = ["uv_build>=0.12.6,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "httpx2>=2.12.0",
    "pytest>=9.1.1",
]
